import { QuoteSession } from "./Client";
import { KeyTo, KeyMayTo, QuotedData, Field, MainField } from './types';
export declare class QuoteChannel {
    private readonly $adapter;
    private fieldList;
    private isChannelActive;
    private pairList;
    constructor(quoteSessionBridge: QuoteSession, pairGroups: string[] | KeyTo<string[]>, fields?: Array<Field>);
    resume(): void;
    pause(): void;
    addFields(field: MainField): void;
    addFields(fields: Array<MainField>): void;
    removeFields(field: MainField): void;
    removeFields(fields: Array<MainField>): void;
    setFields(fields: Array<Field>): void;
    listen(callback: (data: KeyMayTo<KeyTo<QuotedData>>, flags: {
        firstLoad: boolean;
        reload: boolean;
    }) => void): void;
    private pairGroupSerializer;
}
