import { QuoteSession } from "./Client";
import { QuotedData, Field, MainField } from "./types";
export declare class Quote {
    readonly pair: string;
    private readonly $adapter;
    private fieldList;
    private isChannelActive;
    constructor(quoteSessionBridge: QuoteSession, ticker: string, market: string, fields?: Array<Field>);
    resume(): void;
    pause(): void;
    addFields(field: MainField): void;
    addFields(fields: Array<MainField>): void;
    removeFields(field: MainField): void;
    removeFields(fields: Array<MainField>): void;
    setFields(fields: Array<Field>): void;
    listen(callback: (data: QuotedData, flags: {
        firstLoad: boolean;
        reload: boolean;
    }) => void): void;
}
